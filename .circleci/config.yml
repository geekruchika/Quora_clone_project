version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "A first hello"
general:
  branches:
    only:
      - master
      - /development-.*/ # or regexes
